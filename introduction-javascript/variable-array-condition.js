var bottleColor = "yellow";
var waterQuantity = 1;
var isFull = false;

//array
var items = ["bottle", "mug", "paper", "pen"];
iteems.indexOf("logens");
items.push("envelope");
items.push("watch");
items.pop();

//conditionals
if (items.length >= 4) {
  console.log("You have too many stuff on your desk.");
} else if (items.length >= 4) {
  console.log("you have one hali items");
}
